[project]
name = "openpm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "click>=8.2.1",
    "fastapi[standard]>=0.116.0",
    "pip>=25.1.1",
]
